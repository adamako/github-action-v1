name: First CI

on: push

jobs:
  job1:
    name: Frst job
    runs-on: ubuntu-latest
    steps:
      - name: Step one
        uses: actions/checkout@v2
      - name: Step two
        run: |
          pwd
          env | sort

